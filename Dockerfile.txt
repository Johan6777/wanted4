FROM php:8.1-cli

# คัดลอกไฟล์ทั้งหมดจากโฟลเดอร์เดียวกัน (.) ไปยังใน container ที่ /app
COPY . /app

# กำหนดให้ทำงานที่ /app
WORKDIR /app

# เปิดพอร์ต 10000 สำหรับ Render
EXPOSE 10000

# สั่งให้รัน PHP server ด้วยพอร์ต 10000
CMD ["php", "-S", "0.0.0.0:10000"]
